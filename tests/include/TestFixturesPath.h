#pragma once

#include <filesystem>

namespace constellation::test
{
    // Edit this one line to point at wherever your .step fixture files live.
    inline const std::filesystem::path kStepFixturesDir = "/StepFiles";
}
